<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>EDIT - <%= sock.name %></title>
  </head>
  <body>
    <form action="/<%= sock.id %>?_method=PUT" method="POST">
      <li>
        <input type="text" name="name" value="<%= sock.name %>" placeholder="NAME"/>
      </li>
      <li>
        <input type="text" name="img" value="<%= sock.img %>" placeholder="IMAGE URL"/>
      </li>
      <li>
        <input type="textarea" name="description" value="<%= sock.description %>" placeholder="DESCRIPTION"/>
      </li>
      <li>
        <input type="number" name="price" value="<%= sock.price %>" placeholder="PRICE"/>
      </li>
      <li>
        <input type="number" name="qty" value="<%= sock.qty %>" placeholder="QUANTITY"/>
      </li>
      <input type="submit" value="SUBMIT CHANGES">
    </form>
  </body>
</html>
